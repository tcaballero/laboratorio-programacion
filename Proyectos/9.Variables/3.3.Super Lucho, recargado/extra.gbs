procedure Poner__Veces(color,cantidad) { repeat(cantidad) { Poner(color) } }

/*@ATTRIBUTE@block_name@elegir _ cuando _ y si no, _@*/
function elegir_cuando_ySiNo_(valor1, condición, valor2) {
 return (choose
            valor1 when (condición)
            valor2 otherwise
        )
}

